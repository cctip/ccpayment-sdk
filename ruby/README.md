# CCPayment Ruby SDK

> **⚠️ Disclaimer**
> 
> This SDK code was auto-generated by a code generator for reference and educational purposes only.
> **Please verify the code security and refer to the official API documentation before using.**
> The author is not responsible for any issues arising from the use of this code.

Official Ruby SDK for CCPayment API v2.

## Installation

Add this line to your application's Gemfile:

```ruby
gem 'ccpayment-sdk'
```

And then execute:

```bash
bundle install
```

Or install it yourself as:

```bash
gem install ccpayment-sdk
```

## Quick Start

```ruby
require 'ccpayment'

# Create client
client = CCPayment::Client.new('your_app_id', 'your_app_secret')

# Get all assets
response = client.merchant_assets.get_app_coin_asset_list
response['assets'].each do |asset|
  puts "#{asset['coinSymbol']}: #{asset['available']}"
end
```

## Configuration

### HTTP Proxy

```ruby
client.set_proxy('http://127.0.0.1:10808')
```

### Custom Base URL

```ruby
client.base_url = 'https://custom.ccpayment.com/ccpayment/v2'
```

## API Modules

- **basic_info** - Token, fiat currency, and chain information queries
- **merchant_assets** - Merchant asset queries
- **merchant_deposit** - Deposit address generation and deposit record queries
- **merchant_withdraw** - Withdrawal requests and withdrawal record queries
- **merchant_batch_withdraw** - Batch withdrawal management
- **user_assets** - User asset queries
- **user_deposit** - User deposit addresses and deposit records
- **user_withdraw** - User withdrawal requests and withdrawal records
- **user_transfer** - User transfers and batch transfers
- **orders** - Order and Invoice orders
- **checkout** - Checkout and Hosted related
- **swap** - Swap related interfaces
- **utilities** - Webhook, address validation, etc.

## Error Handling

```ruby
require 'ccpayment'

begin
  response = client.merchant_assets.get_app_coin_asset_list
rescue CCPayment::APIError => e
  puts "API Error #{e.code}: #{e.message}"
rescue => e
  puts "Error: #{e.message}"
end
```

## API Documentation

For complete API documentation, visit: https://doc.ccpayment.com
